export default defineNuxtRouteMiddleware(async (to, from) => {
//   const { postTypes, fetchWpTypes } = useWordpress();
  //   console.log("postType: ", postType);
  //   console.log("slug: ", slug);
//   await fetchWpTypes();
});
